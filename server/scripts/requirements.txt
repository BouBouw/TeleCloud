# Python dependencies for server scripts
# Install: pip install -r requirements.txt

# Telegram sync (telegram_full_sync.py)
telethon>=1.36.0

# Whisper transcription / lyrics (transcribe_audio.py)
faster-whisper>=1.0.0

# Vocal isolation (transcribe_audio.py – demucs path)
demucs>=4.0.0

# Noise reduction fallback (transcribe_audio.py – noisereduce path)
noisereduce>=3.0.0
soundfile>=0.12.0

# Docker SDK – used for container management
docker>=7.0.0
